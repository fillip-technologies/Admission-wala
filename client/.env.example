# Client environment variables (Vite).
#   .env             → local dev (uses the Vite proxy, VITE_API_URL=/api/v1)
#   .env.production  → used by `vite build` (absolute backend URL)

# Absolute URL of the backend API (include the /api/v1 prefix).
VITE_API_URL=https://admission-wala-e8wn.onrender.com/api/v1

# WhatsApp floating button — international format, no "+", "0" or spaces.
VITE_WHATSAPP_NUMBER=918409835444
